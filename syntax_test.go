package turbine

import (
	"testing"
)

func TestStandard(t *testing.T) {
	// for path := range inspect.Standard {
	// 	var pkg, err = inspect.Load(path)
	// 	if err != nil {
	// 		t.Errorf("cannot load %s: %v", path, err)
	// 		continue
	// 	}
	// 	for pkg.Nodes.Files
	// }
}

func TestT(t *testing.T) {
	// var pkg, err = inspect.Load("t")
	// if err != nil {
	// 	t.Fatalf("cannot load t: %v", err)
	// }
	// for name, file := range pkg.Nodes.Files {
	// 	fmt.Println(name)
	// 	format.Node(os.Stdout, token.NewFileSet(), file)
	// 	var c nodeConv
	// 	c.tokens = pkg.Tokens
	// 	var s = c.node(file)
	// 	var n = NewNode(s)
	// 	format.Node(os.Stdout, token.NewFileSet(), n)
	// }
}
